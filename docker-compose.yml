services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: reconcile
      POSTGRES_USER: reconcile
      POSTGRES_PASSWORD: reconcile
    ports:
      - '5432:5432'
    volumes:
      - reconcile-pgdata:/var/lib/postgresql/data

volumes:
  reconcile-pgdata:
